function squareEvensRootOdds(str){
  str = str.split('|');
  var cubeArr = str.map(function(n) {
      if(n % 2 === 0){
    return Math.pow(n,2);
} else {
    return Math.sqrt(n);
}
  });

  return cubeArr;
}
